# iris
Iris v2.5.1 in a single script (for exploits)

# How to use
In your code, put this at the very top
```lua
local Iris = loadstring(game:HttpGet("https://raw.githubusercontent.com/break-core/iris/refs/heads/main/iris"))()
```

Then, just use Iris as if you were using the regular module